apiVersion: v1
kind: Service
metadata:
  name: {{ include "mizan.fullname" . }}
  labels:
    {{- include "mizan.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "mizan.labels" . | nindent 4 }}
  ports:
    - {name: https, port: {{ .Values.service.port }}, targetPort: https}
