# WORK_LOG — Mizan Agent Handoff Protocol

> **Read this first, write here last.** Every agent session (Claude Code, Cursor/Codex, test generators) MUST: (1) read `Active Task` + `Next Executable Action` before doing anything, (2) obey `SPEC_v1.md` contracts and `AGENT_ALLOCATION.md` lane boundaries, (3) hold a valid claim (H-1) while working, (4) update this file as its final act. Keep entries telegraphic — this file is optimized for low token overhead. Detail belongs in ADRs, spec, or commit messages, not here.

---

## Active Task

**Wave 1 in progress; T-057 is in REVIEW.** Refused immutable attestation appends now return a typed outcome, never count as completion, and conflicting/legacy occupied slots raise `anchor_attestation_integrity` without repeated TSA calls. Cases 1, 4, 6, and 7 are green together. Next: T-051, then T-052, then the complete wave gate.

## Agent Queue

| # | Task | Lane | Depends on | State |
|---|---|---|---|---|
| T-001 | Ratify SPEC v1.2 + ADR-001..008 (incl. R-002 amendments) | HUMAN | — | DONE |
| T-002 | Repo scaffold per PRD §116 (control-plane/, security/, sdk/, examples/, ui/) + CI skeleton | CODEX | T-001 | DONE |
| T-003 | Postgres schema + migrations for §2 domain models (RLS per ADR-005; typed FKs per I-16; DecisionEvent + chain-head tables) | CODEX | T-001 | DONE |
| T-004 | `/v1/authorize` walking skeleton: token→tenancy, §3.1 enrichment (fail-closed), evaluate stub, ADR_Record write | CODEX | T-003 | DONE |
| T-005 | Policy DSL parser + Cedar compiler spike (ADR-002 benchmark) | CODEX | T-001 | DONE |
| T-006 | Registry CRUD (agents/tools/policies) + list/search endpoints | CODEX | T-003 | DONE |
| T-007 | Invariant suite I-1..I-26 (property-based) + V-1..V-21 tests + approval-SM/epoch fuzzer (§5.2 G1–G9) | CODEX | T-004 | DONE |
| T-008 | Evidence pipeline: ADR_Record + DecisionEvent sequencers, outbox, immutable receipts, object-store segments, signed anchors, `/v1/audit/verify` (ADR-004 amendments A/B) | CODEX | T-003 | DONE |
| T-009 | Approval API + epoch state machine (ADR-007: snapshots, escalate/override atomicity, rejection modes) | CODEX | T-004 | DONE |
| T-010 | Dashboard shell + decision/audit views (PRD §44) | CODEX | T-006 | DONE |
| T-011 | Binding profiles + executor-bound token/lease lifecycle (ADR-008), incl. atomic redemption CAS and SPIFFE match (V-13/V-17/V-20) | CODEX | T-004 | DONE |
| T-012 | Redaction pipeline: DLP attestation, keyed commitments, manifest, reject-on-scan-failure (I-19) | CODEX | T-008 | DONE |
| T-013 | External payload boundary: parser budgets + envelope disposition + versioned projections + drift telemetry (ADR-006) | CODEX | T-004 | DONE |
| T-014 | Claim-ledger CI gate: reject scoped code changes whose change-set does not update WORK_LOG (H-8) | CODEX | T-002 | DONE |
| T-015 | Chain-verification perf harness: 100k-record fixture, checkpointed parallel range verify (<10s) | CODEX | T-008 | DONE |
| T-016 | R-005 F-11 (re-opened): remove the ADR_Record `constraints` read at `repository.py:120`, stop returning `winner.constraints` for non-CONSTRAIN winners, encode the Policy schema "iff" both ways + T-021 gate | CODEX | — | DONE |
| T-017 | R-004 F-2: `system_fail_closed` evidence on engine/dependency failure; wrap policy evaluation; narrow the blanket handler (I-8/V-15) | CODEX | — | DONE |
| T-018 | R-004 F-3/F-7/F-10: bind the real executor at issue, defensive delegation access, bounded security-event connection | CODEX | — | DONE |
| T-019 | R-004 F-6: concurrent duplicate `request_id` returns the prior decision, never `evidence_write_failed` | CODEX | T-016 | DONE |
| T-020 | R-004 F-5: app-terminated mTLS populates `client_cert_spiffe`; deployment contract + ADR-001 delta (I-23) | CODEX | — | DONE |
| T-021 | R-004 F-4: contract drift gates — meta-schema, I-16 typed IDs, SPEC-string reachability, closed-schema producibility; negative fixtures in CI | CODEX | — | DONE |
| T-022 | R-004 F-8/F-9: decompose the over-cited integration test, raise `test_execution.py` coverage, re-chain the in-memory repo, reconcile doc drift | CODEX | T-017, T-018 | DONE |
| T-023 | Load & latency harness: SPEC §7 p95/throughput/outbox-lag on deployment-class Linux; JSON artifacts (closes B-2/B-6 rerun obligation) | CODEX | T-022 | READY |
| T-024 | Adversarial suite: token replay, cross-tenant fuzz, chain tamper, prompt-injection corpus; nightly CI (PRD §39/§62) | CODEX | T-022 | READY |
| T-025 | ADR-004 Amd. G.1: four key roles, KMS/HSM `KeyProvider`, published `/v1/audit/keys` keyset, additive never-retroactive rotation, production refuses `local://` at startup | CODEX | T-033 | DONE |
| T-026 | Outbox drain operations: runner, backpressure, poison handling, lag SLO, SIEM delivery | CODEX | T-022 | READY |
| T-027 | Threat model v1 — CLAUDE draft at `threat-models/TM-001-control-plane-v1.md`; awaiting HUMAN ratification of three judgement calls in §6 (adversary A-8 scope, R-1 disposition, R-7 deferral to TM-002). Produced one new engineering finding (R-2 → T-054) | HUMAN | — | READY(draft) |
| T-028 | Constrained-execution specification incl. executor-side enforcement contract (B-10 Option B) | HUMAN | — | PARKED(v1.4) |
| T-029 | R-005 F-12: benchmark artifact discipline — every `benchmarks/` module writes `benchmarks/results/<name>-<sha>.json` with measurement, host, SHA; CI fails on a missing artifact | CODEX | — | DONE |
| T-030 | R-005 F-14: anchor chaining/ordering/density — `prev_anchor_hash`, monotonic `anchor_number`, `covered_record_count`; verifier checks anchor-set continuity; three negative fixtures | CODEX | — | DONE |
| T-031 | R-005 F-15: provenance tri-state (`Recorded`/`NotApplicable`/`Unpopulated`) + `Observed`/`Declared` qualifier inside the signed payload; `NotApplicable` never inferred; new ADR-010 | CODEX | T-030 | READY |
| T-032 | R-005 F-16: evidence export bundle + standalone `scripts/verify_evidence_export.py` — no `mizan_control_plane` import, no DB, no network; clean-virtualenv CI job | CODEX | T-030 | DONE |
| T-033 | R-005 F-13 (unblocked half): `AnchorProvider` seam + labelled unattested dev provider; mandatory I-11/ADR-004 correction so the tree stops asserting external anchoring it does not have | CODEX | T-030 | DONE |
| T-034 | R-005 F-17: `docs/contracts/EVIDENCE_BINDING_INDEX.md` — every ADR_Record-cited field mapped to the hash committing to it, gate-enforced; close what the enumeration finds unbound | CODEX | T-031 | READY |
| T-035 | R-005 F-18: CI anti-rot gates — monotonic drift-gate/fixture count manifest, and skipped-is-not-passed for `postgres-contract` and the T-032 verifier job | CODEX | — | READY |
| T-036 | ADR-004 Amd. G.2: RFC 3161 provider + `attestations[]` + async SLO/breaker + customer countersignature; verifier validates offline against an operator-supplied trust root; lift the I-11 waiver. **R-006 V-3 binds:** assurance is *derived* from validated tokens only — the manifest's `assurance` block is a claim under test, and claimed≠derived is a verification failure | CODEX | T-033, T-025 | DONE (CP-B passed 2026-08-26) |
| T-037 | R-005 §10: `--report report.html` — the auditor's artifact; verdict, what it proves, and **what it does not prove** at equal prominence; golden-output test | CODEX | T-036, T-038 | READY |
| T-038 | R-005 §10: Merkle inclusion proofs — anchor `merkle_root` with RFC 6962 domain separation, `/v1/audit/inclusion/{decision_id}`, standalone `--inclusion` verification (ADR-004 Option-2 path) | CODEX | T-030, T-036 | READY |
| T-039 | R-005 §10: RFC 6962 consistency proofs between successive anchors — append-only proven cryptographically, not only by numbering | CODEX | T-038 | READY |
| T-040 | R-005 §10: `make attack` adversary drill over a real bundle; committed deterministic output that **must** include the uncaught attack classes | CODEX | T-036, T-039, T-024 | READY |
| T-041 | R-006 V-1/V-2: benchmark artifact provenance — `commit_sha` must name a real commit, record `worktree_clean`, CI rejects dirty/unresolvable artifacts; register `MIZAN_BENCHMARK_*` keys in SPEC | CODEX | — | DONE |
| T-042 | R-006 V-4/V-5: verifier binds **every** in-range anchor `head_hash` to its record and pins the left edge via the anchor ending at `range_start-1`; PASS line stops crediting unsigned checkpoints as evidence | CODEX | — | DONE |
| T-043 | R-006 V-6: operator entry point that produces a bundle + live-Postgres end-to-end test (authorize → drain → anchor → export → run `verify_evidence_export.py` as a subprocess). Closes the self-certifying-fixture gap | CODEX | — | DONE |
| T-044 | **Stage 4 head:** determinism enumeration + `evaluator.json` pinning contract + new **ADR-011**; produces the list of what is and is not replayable and ships no replay. May legitimately kill T-045..T-048 | CODEX | CP-D | PROPOSED |
| T-045 | Stage 4: replay bundle — `policies/`, `contexts/`, `evaluator.json`, each checked against the pin that already exists in the ADR_Record (I-8 `content_hash`, `context_hash`, `evaluator`) | CODEX | T-044, **B-13** | PROPOSED |
| T-046 | Stage 4: `--replay` in the standalone verifier — per record `REPLAYED` / `NOT REPLAYABLE(reason)` / `DIVERGED(recorded vs recomputed vs deciding clause)`, offline | CODEX | T-045 | PROPOSED |
| T-047 | Stage 4: the trusted-input ledger — `Recomputed` vs `Still trusted`, every non-derivable field named with its T-031 provenance class. This is the deliverable, not the replay engine | CODEX | T-046, T-031 | PROPOSED |
| T-048 | Stage 4: divergence corpus + `make replay-attack` — cryptographically flawless records recording the wrong decision, which every Stage 3 layer passes; must include a case `--replay` also misses | CODEX | T-046, T-040 | PROPOSED |
| T-049 | R-007 V-11 (**two defects**): (a) union the sidecar over the signed payload keyed by `(type, authority)` — the signed payload is the authoritative roster, so fix both copies of `row.get("attestations") or payload...` (`verify_evidence_export.py:184`, `evidence_export.py:106`), and an undeclared sidecar authority is a failure; (b) invert the anchor-level precedence so `pending` outranks `verified_external` (`verify_evidence_export.py:224-230`, `evidence_export.py:109-117`) — the union alone leaves the anchor deriving `rfc3161`. Mixed-**authority** fixture. Keep the two implementations independent (rule 12) | CODEX | — | DONE |
| T-050 | R-007 V-14: validate before recording — recompute the imprint and verify the token against configured roots before writing `attested`; a token that fails stays `pending` with a named reason; production refuses non-TLS TSA endpoints | CODEX | — | DONE (introduced V-16 → T-055) |
| T-055 | R-007 V-16 (**introduced by T-050, blocks CP-B**): the worker records every `obtain()` return value, so a failed validation writes a permanent `pending` row into `mizan.anchor_attestations` — PK `(tenant_id, anchor_id, authority, attestation_type)`, `INSERT ... ON CONFLICT DO NOTHING`, `UPDATE`/`DELETE` revoked and trigger-rejected. The retry is then skipped as `finalized` (`attestation.py:164-166`), the eventual real token is silently swallowed, the anchor is hidden from the pending-SLO breaker, and `completed += 1` counts a failure as a completion. **One transient fault permanently bars an anchor from satisfying I-11.** Fix inside append-only semantics — the table must stay immutable. `test_worker_records_validation_failure_as_named_pending_sidecar` must change with it | CODEX | — | DONE (outcome-only append; introduced nothing, exposed V-17 → T-057) |
| T-057 | R-007 V-17 (**exposed by T-055, blocks CP-C**): `record_anchor_attestation` (`evidence.py:613-628`) is `INSERT ... ON CONFLICT DO NOTHING` and returns `None`, so the worker cannot distinguish an append from a silent refusal and increments `completed` either way. T-055's correctness rests on the `(anchor, authority, type)` slot being empty and nothing checks that it is: an anchor carrying a pre-fix pending row is now hit against the TSA on **every pass forever** — gate case 7 mints and discards a valid token three times while reporting three completions. Return the rowcount; a refusal is not a completion; a refusal with a *differing* document is a named integrity event, not a no-op; an occupied slot must stop the loop. Table stays immutable | CODEX | — | REVIEW |
| T-059 | R-007 V-18 (**new**): the anchor core digest — `payload` minus `{attestations, object_key, object_version}`, JCS, SHA-256 — is the most load-bearing definition in the product and appears only in `evidence.py` and `verify_evidence_export.py`, **nowhere normative**. So the only implementation of the bundle format is Mizan's own, and "verify it yourself offline" means "run our script". Write `docs/spec/EVIDENCE-BUNDLE-FORMAT.md` implementable by someone who has never read the verifier — canonicalisation, every hashed byte, the core projection as a *closed* rule, chain linkage, attestation legality, derived assurance, trust roots from the operator never the bundle, and **what the format does not prove** (TM-001 pre-chain omission + withheld final anchor) at equal prominence. Plus `tests/fixtures/conformance/` with a machine-readable expected verdict per bundle, run in CI. No second implementation in this task | CODEX | T-056 | READY |
| T-058 | **New** — mutation resistance as a property, not a checklist: for a valid bundle, no single-byte mutation of any file yields exit 0. Seeded, deterministic, bounded, offline, in CI. **Every survivor is a finding** — enumerate and classify each as benign (semantically identical) or a hole, and file the holes; do not narrow the sample to reach green. Distinct from T-040, which covers attacks a human thought of | CODEX | T-059 | READY |
| T-060 | **New** — what verification costs and where it stops being linear: wall clock and peak RSS across at least three orders of magnitude of chain length, artifacts under `benchmarks/results/` per rule 6. State the *shape* (linear in records? memory bounded or proportional?) and **the size at which it breaks**, not the largest size that worked. Contrast with T-038's O(log n) inclusion proof — if linear cost is bad, that is the argument for making inclusion proofs the primary auditor interface. Do not tune in the same change-set | CODEX | T-038 | READY |
| T-056 | TM-001 R-6 (promoted from unqueued): real RFC 3161 **interoperability** — obtain tokens from **two independent public TSAs** over a digest this repo computes, commit tokens + roots + provenance as offline fixtures, verify a bundle carrying them with no network, and record every incompatibility hit (policy OIDs, hash negotiation, `-cert`, chain construction, nonce/`Accept` requirements). No network in CI. If no public TSA is reachable, **park** — do not substitute another local CA and call it interop. Converts "we implement RFC 3161" into "tokens from authorities we do not control verify offline against roots we did not issue" | CODEX | T-055 | READY |
| T-051 | R-007 V-12: `cannot check` ≠ `check failed` — catch `FileNotFoundError`/`OSError` into a distinct failure, separate the four causes, no traceback, probe `openssl ts` up front, and add an **attested** bundle to the CI offline-verifier job with its toolchain declared | CODEX | T-049 | READY |
| T-052 | R-007 V-13/V-15: attestation runner + breaker wiring so `MIZAN_ANCHOR_ATTESTATION_MAX_PENDING_SECONDS` has an enforcement point (owner may merge into T-026); evaluate the SLO outside the `except` branch; narrow `except Exception`; make `test_worker_opens_breaker_after_tsa_outage_exceeds_slo` assert the breaker opened. **Sequence after T-055** — the breaker's job is to notice anchors stuck pending, and T-055 decides what a stuck anchor looks like | CODEX | T-055 | READY |
| T-053 | R-007 V-8/V-9 custody honesty: `LocalKeyProvider` refuses `environment == "production"` regardless of URI scheme; add `custody` (`development-derived`/`kms`/`hsm`) to the keyset and `required_key_fields`; verifier prints `KEY CUSTODY: publicly derivable development key — this bundle is forgeable by anyone who reads it.` | CODEX | — | READY |
| T-054 | TM-001 R-2: the audit commitment HMAC key has a contract (`MIZAN_AUDIT_HMAC_KEY_REF`, ADR-004 *"held under separate authority"*) and no custody — it is not one of G.1's four `KeyRole`s, so `keys.py:65` cannot cover it, there is no KMS/HSM adapter, no production refusal of development custody, no keyset publication, and no implemented rotation. Add `audit-commitment` as a fifth role, route `security/mizan_security/redaction.py` through the provider, publish it with its `custody` field, implement the rotation §8 already promises. The key MACs rather than signs — if `KeyProvider` needs a contract change rather than an addition, stop and file a blocker | CODEX | T-053 | READY |

States: `READY → IN_PROGRESS(claim) → REVIEW → DONE` | `BLOCKED(dep)` | `PARKED(reason)`

## Claim Ledger

One row per active claim. A task is `IN_PROGRESS` **iff** it has a live row here. Empty = nothing claimed.

| task_id | claimed_by | claim_token | claim_version | claimed_at | heartbeat_at | lease_expires_at | base_commit |
|---|---|---|---|---|---|---|---|

## Blockers & Dependencies

- **B-1 (resolved 2026-08-25):** User ratified T-001 in all required Product/Architecture, Cybersecurity, and Compliance/Business roles. SPEC v1.2, R-002, and ADR-001..008 are accepted as the implementation baseline.
- **B-2 (resolved by T-005):** Cedar 4.8.7 cached handles measured 6,896 eval/s and p99 0.1741 ms over 5,000 evaluations on the M3 Max host. The live PostgreSQL integration suite separately gates RLS-scoped lookup + evaluation below 50 ms p99. ADR-002 is ACCEPTED; deployment-class Linux sizing must rerun the benchmark.
- **B-3 (resolved by T-002):** Git repository initialized on `main`; ratified foundation recorded in root commit `da7c83d`.
- **B-4 (resolved 2026-08-25):** T-001 ratification included Compliance/Business sign-off on ADR-007 `rejection_mode` and override-authority semantics.
- **B-5 (resolved in v1.2):** Control domains come from a reviewed/versioned Mizan role-registry mapping populated from IdP data; epoch snapshots pin the mapping version. Ratification remains under B-4/T-001.
- **B-6 (resolved by T-008):** Four sharded streams measured 2,725 transaction-level allocations/second over 2,000 operations with p99 2.0087 ms on the M3 Max development host. ADR-004 is ACCEPTED; deployment-class Linux sizing must rerun the benchmark.
- **B-7 (resolved 2026-08-25):** R-003 ratified a typed, independently controlled review epoch with no vote carry-forward and no recursive `review_required` rejection.
- **B-8 (resolved 2026-08-25):** R-003 ratified bounded transient tool arguments at authorization/redemption plus fresh authoritative enrichment before execution.
- **B-9 (resolved 2026-08-25):** R-003 ratified a normative semantic policy hash excluding only lifecycle governance fields.
- **R-003 (ratified 2026-08-25):** User ratified B-7/B-8/B-9 in all Product/Architecture, Cybersecurity, and Compliance/Business roles.
- **B-10 (resolved 2026-08-25):** Human owner ratified R-004 **Option A** — the evaluator rejects `CONSTRAIN`/`REDACT`/`ESCALATE` with `NOT_IMPLEMENTED` and an auditable `DENY` ADR_Record, and the dead `constraints_hash` binding is removed. Constrained execution (Option B) is deferred to v1.4 as T-028. T-016 unblocked; CODEX implements Option A exactly and does not extend the `ADR_Record` schema.
- **B-11 (resolved 2026-08-25):** Custody ratified as **KMS/HSM with four separately-held key roles**, a published verification keyset served at `/v1/audit/keys` and copied into every export bundle, and additive rotation that **never re-signs history** (a re-signed corpus is byte-indistinguishable from a forged one). `local://` keys are development-only and a production process carrying one refuses to start. Contract: ADR-004 Amendment G.1. Implemented by T-025.
- **B-12 (resolved 2026-08-25):** The external attesting party ratified as **RFC 3161 timestamping, mandatory on every production anchor** — not an enterprise tier, not deferred — with **customer countersignature additive** for enterprise, multiple independent authorities supported, and blockchain still rejected. The TSA sees the SHA-256 of the canonical anchor payload and nothing else. Attestation is asynchronous and never on the authorization hot path; breaching `MIZAN_ANCHOR_ATTESTATION_MAX_PENDING_SECONDS` opens the evidence breaker, and no stream with a pending attestation may be described as externally anchored. Trust roots are supplied by the verifier's operator, never shipped inside a Mizan bundle. Contract: ADR-004 Amendment G.2. Implemented by T-036.
- **R-005 §8 (ratified 2026-08-25):** Human owner ratified B-11 + B-12 together and adopted the four falsification tests plus the second founder test (`docs/product/FALSIFICATION_TESTS.md`). The strategic sequencing question in R-005 §2 remains open, belongs to the owner, and gates no engineering task.
- **R-004 (closed 2026-08-25):** Implementation review of the T-001..T-015 baseline. Ten findings dispositioned in `docs/reviews/R-004-implementation-review-disposition.md`. Independent §6 validation re-ran every gate and sampled three of seven pre-fix failure claims; five of seven tasks accepted, F-11 and F-12 raised (see R-005 §1).
- **B-13 (open, HUMAN, 2026-08-25):** *What may leave the trust boundary in a replay bundle?* Stage 4 recomputation needs the `EvaluationContext`, which carries tool arguments and therefore customer data. Options: **(1) commitments only** — recommended; replay works wherever the policy does not read a redacted field, everything else is `NOT REPLAYABLE` with a named reason, nothing sensitive leaves; **(2) plaintext contexts** — full replay coverage, and every export becomes a data-transfer event with residency/DPA consequences; **(3) operator-selectable per export**, defaulting to (1), plaintext requiring an explicit flag, a named authorizing operator, and its own AuditTrail event. Adjacent to tenant isolation (H-7). Blocks T-045; does **not** block T-044, which is deliberately sequenced first because it can kill the rest cheaply. Proposal: `docs/product/STAGE-4-DECISION-REPLAY.md`.
- **R-007 (closed 2026-08-26):** Independent validation of Stage 3 checkpoint CP-B in `docs/reviews/R-007-CP-B-validation.md`. **CP-B not passed; T-036 held in REVIEW.** The cryptographic core was reproduced independently for the first time — a local standards-compliant TSA minted a real token over the digest the verifier itself reconstructs, and the standalone verifier PASSed the bundle as a subprocess with no monkeypatching on either side, withdrawing its limitation line; an unrelated CA produced a clean FAIL. Rule-9 sampling was honest: the real pre-fix `2e4e81e` golden bundle is rejected with exactly the claimed message. Five findings: V-11 a pending co-authority is dropped by `row.get("attestations") or payload...` and the stream is then reported externally anchored, contradicting ADR-004 G.11 written in the same commit (**blocks CP-B**, T-049); V-14 `obtain()` writes `attested` on unvalidated bytes and the exporter derives assurance from that status field, so V-3 is closed at the verifier and re-opened inside the boundary (**blocks CP-B**, T-050); V-12 the verifier raises an uncaught `FileNotFoundError` when OpenSSL is absent and collapses four distinct causes into one message, while CI never runs the RFC 3161 path (T-051); V-13 the attestation worker, breaker, and SLO config have zero production callers, so B-12's breaker clause has no enforcement point — failure is safe, and T-026 is the same shape (T-052); V-15 a test asserts less than its name claims (T-052). V-8/V-9 confirmed still open, disclosed not hidden, and **reclassified out of H-7** — G.1 already ratified custody over naming, so they are engineering tasks (T-053). Nothing in CP-B waits on a human. Hostile-party answer **still no**, but now for wiring and reporting reasons rather than cryptographic ones.
- **R-006 (closed 2026-08-25):** Independent validation of Stage 3 checkpoint CP-A in `docs/reviews/R-006-CP-A-validation.md`. T-016/T-029/T-030/T-032 accepted DONE; every gate re-run, all four pre-fix SHAs sampled and honest, the standalone verifier confirmed to load no `mizan` module. Seven findings: V-1 config keys unregistered (H-3 amended post-hoc + T-041), V-2 forgeable benchmark SHA and unchecked worktree (T-041), V-3 manifest assurance declared not derived (binds T-036), V-4 intermediate anchors unbound to exported records (T-042), V-5 unsigned checkpoints credited as evidence in the PASS line (T-042), V-6 the export path never run against the real pipeline and no operator entry point (T-043), V-7 three of four pre-fix demonstrations were `ImportError` (standing rule 9).
- **R-005 (open, 2026-08-25):** Critical evaluation of the external "Evidence Plane First" brief in `docs/reviews/R-005-evidence-plane-brief-disposition.md`. Six findings F-13..F-18 accepted (T-030..T-035); seven claims rejected with evidence (R-1..R-7) — chiefly that the brief is written against PRD v1.0 and describes as open several decisions this repo ratified at T-001/T-013, and that its Rust "verbatim port" costing does not survive the language boundary. Source-repository metrics are unverified and must not be re-quoted.

## Next Executable Action

> **CP-B is passed. Work order: `docs/handoff/CODEX-CP-C-RUN.md`** — eleven tasks, four waves, one
> commit each, **no mandatory stop before CP-C**.
>
> **Acceptance gate: `uv run python docs/reviews/reproductions/R-007-cpb-attestation.py`** — now
> **seven** cases. It prints `CP-B ... PASSED` and still exits non-zero while 5 (T-051) or 7 (T-057) are
> open; the exit code is the CP-C gate now. Case 1 must never go red — it is the only place in the tree
> that executes both halves of the signer/verifier digest agreement. **Cases 6 and 7 are a pair**, as 4
> and 6 are: 6 says a failed attempt must stay retryable, 7 says the retry must be able to land. Do not
> green 7 by re-introducing the `finalized` skip.
>
> **Run the full gate at the end of every wave. A previously-green case going red is a stop-and-report,
> immediately, without starting the next task.** That is the self-service checkpoint that buys the run
> its lack of a mandatory stop.
>
> `… → T-036 ✓` **(CP-B PASSED)** `→ T-049 ✓ → T-050 ✓ → T-055 ✓ →` **W1** `T-057 → T-051 → T-052 →`
> **W2** `T-056 → T-059 → T-058 →` **W3** `T-053 → T-054 →` **W4** `T-038 → T-039 → T-060` **(CP-C)**
> `→ T-031 → T-034 → T-035 → T-024 → T-040 → T-037` **(CP-D)** `→ T-026 → T-023`
>
> T-053 gates **delivery**: until it ships, any bundle handed to a design partner is forgeable by its
> recipient and does not say so.
>
> **Park freely.** Eleven tasks is a lot of surface and at least one will be wrong as specified. Parking
> with a reason is a result. **Escalate under H-7 rather than deciding:** any contract *change* to
> `KeyProvider` (T-054), and anything moving approval semantics or tenant isolation.
>
> Standing rules unchanged, plus one added by R-006 V-7:
>
> 7. **Rule 6 is mechanical.** No number anywhere without its `benchmarks/results/` artifact.
> 8. **Every fix ships with the test that fails on the pre-fix commit, and that SHA is named here.** All four CP-A claims were sampled and all four held.
> 9. **New — a guarantee is demonstrated by rejecting the old output, not by an `ImportError`.** Where a task adds a *guarantee* rather than fixing a *behaviour*, the pre-fix demonstration must show the **new gate rejecting the artifact the old code produced**. "The module did not exist yet" proves the code is new; it does not demonstrate the defect. Keep the pre-fix SHA line and add this to it.
> 10. **Report what did not work.** CP-A's report disclosed that `uv --offline` failed and the network was used before isolation. That disclosure is why the rest of the report was believed. Keep doing it.
> 11. **New — a test's name is a claim.** If the name says the system does X, the body must assert X. R-007 V-15 found a test named `..._opens_breaker_after_tsa_outage_exceeds_slo` that asserts only a completion count and never checks that the breaker opened. Second checkpoint running to find a test that names more than it checks.
> 12. **New — a cross-check between two copies of one expression is not a cross-check.** R-007 V-11: the exporter and the verifier contained the identical `or`, so the V-3 claimed-vs-derived gate compared a wrong answer to the same wrong answer and passed. Where a check exists to catch a producer's error, the checker must not share the producer's code path.

---

## Transition Hooks (automated handoff, no orchestrator stalls)

**H-1 · Claim (atomic lease).** Work requires a row in the Claim Ledger with `task_id, claimed_by, claim_token, claim_version, claimed_at, heartbeat_at, lease_expires_at, base_commit`. Only the holder of `claim_token` may heartbeat, complete, or release. Default lease 4h; heartbeat by updating `heartbeat_at` in any commit. **Reclaim requires both** an expired `lease_expires_at` **and** a compare-and-swap on `claim_version` (read version N, write N+1 in the same change-set; a losing writer sees N+1 and aborts). No "any agent may reset a stale row" — that permitted duplicate work and competing commits.
**H-2 · Complete:** finishing agent (a) releases its claim row, (b) flips the queue row to `REVIEW`, (c) rewrites `Next Executable Action` to the single best next step (anti-stall — never leave it stale or empty), (d) appends a Log entry, (e) unblocks dependent rows (`BLOCKED(T-00X)` → `READY` when T-00X hits DONE-or-REVIEW if the dependency is interface-only).
**H-3 · Contract touch:** any change to schemas/endpoints/events/state machines/invariants requires an ADR delta *in the same change-set*; otherwise the change is invalid and the next agent reverts it. Spec wins over code, always. **Config keys (amended 2026-08-25 by R-006 V-1, post-hoc after T-029 shipped two unregistered keys):** *every* config key must be registered in `SPEC_v1.md` in the same change-set, without exception; an **ADR delta is additionally required only** where the key changes contract-bearing runtime behaviour (anything an auditor, a tenant, or a security review could observe). A benchmark output directory registers; it does not need an architecture decision. The amendment is recorded as post-hoc deliberately — it narrows an over-broad rule rather than excusing the violation, and T-041 registers the two keys.
**H-4 · Lane violation:** work outside your lane (per `AGENT_ALLOCATION.md`) → stop, park the task `PARKED(lane)`, note it here. Never "just fix" cross-lane.
**H-5 · Test gate:** CURSOR-lane code merges only with TEST-lane (or pre-existing) tests green. CLAUDE-lane security-critical code additionally requires the invariant suite (T-007 onward) green, including the V-rule tests.
**H-6 · Session end:** final act of every session = update this file. A session that edited code but not WORK_LOG is treated as unfinished.
**H-7 · Human escalation:** decisions touching money movement, approval semantics, crypto, key management, or tenant isolation escalate to HUMAN lane — add a `Blockers` row, park, continue with other READY work (don't idle).
**H-8 · CI enforcement (not honour system):** the repo's CI rejects a change-set that touches lane-scoped code paths without a matching `WORK_LOG.md` update and a live claim row (T-014). A local pre-commit hook may mirror this for fast feedback, but **CI is authoritative** — local hooks are bypassable with `--no-verify`, so they can never be the control.

---

## Log (newest first, one line each: `date · lane · task · what · next`)

- 2026-08-26 · CLAUDE-rigor/CODEX · T-057 · Made immutable attestation writes return `appended`/`unchanged`/`conflict`; only a landed append counts, differing documents and legacy pending occupants open the named `anchor_attestation_integrity` breaker, and occupied slots stop TSA retries without mutation. Chose refuse-and-escalate because an additive supersession relation would expand the evidence format and could be mistaken for assurance; ADR-004 G.13 records that choice. Pre-fix `e76b2b6` failed `test_worker_escalates_occupied_pending_sidecar_without_retrying_forever` and `test_worker_does_not_count_store_refusal_and_names_conflict`; 16 attestation unit tests, 13 live PostgreSQL tests (including real rowcount/idempotence/conflict), Ruff, and reproduction cases 1/4/6/7 pass. What did not work: case 5 remains red for T-051; pre-fix poisoned rows remain immutable and require operator intervention rather than automatic repair. Hostile DB+signing-key answer is **yes only for independently attested finalized anchors**; the integrity breaker prevents false completion but cannot repair attacker-written legacy state · next: T-051
- 2026-08-26 · CLAUDE-rigor/CODEX · T-055 · Chose outcome-only append semantics: the immutable `anchor_attestations` table receives only validated `attested` outcomes, while failed attempts leave the signed payload's durable pending state visible and retryable; only `attested` sidecars suppress retries or increment `completed`, and ADR-004 G.2/G.12 records the contract. Pre-fix `d4d57c7` failed `test_worker_does_not_persist_validation_failure_and_retries_to_attested` because pass one returned 1 and wrote the pending failure; the fixed test proves pass one writes/counts nothing and pass two attests, while a separate regression proves a pending sidecar is nonterminal. R-007 cases 1/3/4/6, 170 unit/property, 12 live PostgreSQL, Ruff, and five drift gates pass. What did not work: the complete reproduction remains nonzero solely on case 5 (T-051, out of scope); diagnostics for failed attempts are intentionally not persisted, and any pending sidecar already written by pre-fix code cannot be repaired in place without violating immutability, so an already-deployed poisoned row needs a separately designed additive recovery path. Hostile DB+Mizan-signing-key answer is **yes only for finalized anchors carrying independently rooted TSA tokens**; T-055 preserves that boundary but adds no protection for pending/withheld anchors · next: STOP for CP-B re-run; do not claim T-051/T-038
- 2026-08-26 · CLAUDE-rigor/CODEX · T-050 · The asynchronous RFC 3161 provider now validates the returned token's recomputed imprint against operator-supplied trust roots before returning `attested`; invalid/uncheckable responses are append-only named `pending` sidecars, production requires `MIZAN_ANCHOR_TSA_TRUST_ANCHORS` plus HTTPS TSA endpoints, the factory wires those roots, and SPEC §8/ADR-004 G.2 carry the H-3 delta; pre-fix `c4ea7b517d9aa7e4003fb55b300a4fb9652b7e1f` failed `test_unvalidated_tsa_response_remains_pending_with_named_reason` with garbage recorded as `attested`; a real local OpenSSL TSA token passed both worker-side and stranger-verifier validation, R-007 cases 1–4, 169 unit/property, 12 live PostgreSQL, Ruff, and five drift gates pass; what did not work: the full R-007 harness remains nonzero solely on case 5's missing-OpenSSL traceback (T-051, out of scope), and no public-network TSA was contacted; hostile DB+Mizan-signing-key answer at this worker boundary is **yes only when the operator trust root remains independent**, but the repository-wide answer remains **no** pending T-052 runner enforcement and T-053 custody disclosure · next: STOP for CP-B re-run/disposition
- 2026-08-26 · CLAUDE-rigor/CODEX · T-049 · Reconstructed effective attestations independently in the exporter and stranger-owned verifier (deliberate rule-12 duplication): the Mizan-signed `(type, authority)` roster is authoritative, matching append-only sidecars overlay completions, undeclared sidecars fail, named pending authorities remain visible, and pending outranks verified external within an anchor; pre-fix `0a2eb2b519c651cadaa2f745e985d5ba2facf422` failed `test_exporter_mixed_authority_anchor_uses_pending_as_weakest_state` by claiming `rfc3161`/external timestamp on the old output, while R-007 cases 1/2/3, 165 unit/property, 12 live PostgreSQL, Ruff, and five drift gates pass; no contract delta because ADR-004 G.11 was already binding; what did not work: the complete R-007 harness still exits nonzero on intentionally open cases 4 and 5; hostile DB+signing-key answer remains **no** until T-050 prevents unvalidated sidecars from being recorded as attested (and T-052 supplies the runner) · next: T-050 CODEX, then stop for CP-B re-run
- 2026-08-26 · CLAUDE · **CP-B PASSED** · Re-ran the gate against `e76b2b6` and **passed CP-B**: cases 1, 2, 3, 4 and 6 green together, which is the R-007 §5 criterion as amended by both re-runs. **T-055 and T-036 accepted DONE.** This pass reproduced the supporting gates rather than accepting them, closing the limitation the previous entry disclosed: `ruff` clean, `make check` clean (30 boundaries, 14 JSON blocks, 13 schema IDs, five drift gates), 170 passed / 12 skipped, and rule 8 sampled directly — checked out `d4d57c7`'s `attestation.py` and confirmed **both** new tests fail on it (`test_worker_does_not_persist_validation_failure_and_retries_to_attested`, `test_worker_does_not_treat_pending_sidecar_as_finalized`). T-055's outcome-only append choice is the right one and the commit message defended it; CODEX additionally routed the non-`attested` branch into the SLO breaker, which the order asked for only implicitly. Then took the independent path one call site past where case 6 stops looking and found **V-17**: `record_anchor_attestation` (`evidence.py:613-628`) is `INSERT ... ON CONFLICT DO NOTHING` returning `None`, so the worker cannot tell an append from a silent refusal and increments `completed` either way — T-055's correctness rests on the `(anchor, authority, type)` slot being empty and **nothing checks that it is**. CODEX disclosed that a pre-fix pending row cannot be repaired; the cost it did not disclose is that T-055 removed the `finalized` skip, so such an anchor is now hit against the TSA on *every pass forever*. Added **gate case 7**: three passes over a legacy pending row against an entirely healthy TSA — *"3 token(s) minted and discarded; the worker reported 3 completion(s)"*, sidecar still `pending`. Also recorded **V-18**, not a code defect: the anchor core digest (`payload` minus `{attestations, object_key, object_version}`, JCS, SHA-256) exists in `evidence.py` and `verify_evidence_export.py` and **nowhere in SPEC or ADR-004**, so the only implementation of the bundle format is Mizan's own and "verify it yourself, offline" means "run our script" — the second founder test applied to the verifier answers no. Neither V-17 nor V-18 causes a false claim of external anchoring, so both are pre-CP-C rather than CP-B blockers, and the gate now prints which checkpoint each open case blocks. Issued `docs/handoff/CODEX-CP-C-RUN.md`: eleven tasks in four waves with no mandatory stop before CP-C, the wave-end gate run as a self-service checkpoint, and four new tasks — **T-057** (V-17), **T-059** (V-18: a normative bundle format plus a conformance corpus, no second implementation yet), **T-058** (no single-byte mutation of a bundle verifies; every survivor is a finding), **T-060** (what verification costs and the size at which it breaks, measured not tuned). Also re-framed **T-038** in the order: `TM-001` names pre-chain omission and a withheld final anchor as NOT COVERED, and an RFC 3161 token proves an anchor existed by time T but never that no other anchor exists — a retained inclusion proof is the only mechanism in the design that lets a third party prove a record must be in a chain it does not control, so T-038 is the answer to the largest remaining hole, not an item on a list. Limitation: the 12 live-PostgreSQL tests were again accepted from CODEX's report rather than re-run, and case 7 models `ON CONFLICT DO NOTHING` by hand from the migration because the gate must run without a database — T-057 is required to cover the real rowcount behaviour against a live table. Hostile DB+signing-key answer **still no**, and the reason has moved: the crypto boundary holds for finalized anchors under an independent trust root, and what remains is wiring (T-052), custody disclosure (T-053), and omission (T-038) · next: CODEX wave 1 — T-057 → T-051 → T-052
- 2026-08-26 · CLAUDE · CP-B re-run · Re-ran the CP-B gate against `d4d57c7` and **did not pass it**, on one blocker instead of two. **T-049 and T-050 accepted DONE** — both defects were real, both fixes correct, the two implementations were kept independent as rule 12 requires, and CODEX added two things the order did not ask for (production refuses plaintext TSA endpoints, and the verifier now names pending authorities in its per-anchor line). Cases 3 and 4 are green. Then took the fresh independent path §"What the CP-B re-run will do" promised and found **V-16, a defect T-050 introduced**: `obtain()` correctly stopped raising and now returns a `pending` dict with a named `failure_reason`, but the worker at `attestation.py:175` records **every** return value, and the store is `mizan.anchor_attestations` — PK `(tenant_id, anchor_id, authority, attestation_type)`, `INSERT ... ON CONFLICT DO NOTHING`, `UPDATE`/`DELETE` revoked *and* trigger-rejected. So one transient TSA fault writes a permanent `pending` row; the retry is then skipped as `finalized` at `attestation.py:164-166`, the eventual real token would be silently swallowed by the conflict clause anyway, there is no repair path because the table is correctly immutable, `completed += 1` reports work that did not happen, and the anchor stops looking pending to the one breaker that exists to notice a stalled attestation — **a single network blip permanently bars an anchor from ever satisfying I-11**. Promoted it into the gate as **case 6**: two ordinary worker passes against a TSA that returns garbage once and then mints real tokens, with a fake store reproducing the migration's exact PK and `ON CONFLICT DO NOTHING` semantics, nothing monkeypatched on either side — the counter reports **one** TSA call, so the retry did not fail, it did not happen. Cases 4 and 6 are now a pair and the order says so: greening 6 by reverting T-050 reddens 4 and is a trade, not a fix. Also noted `test_worker_records_validation_failure_as_named_pending_sidecar` is not a bad test — its claim is true and too small, it never asked what the second pass does — and required it to change with the fix. Issued `docs/handoff/CODEX-CP-B-CLOSEOUT.md` (T-055 blocker, T-051, T-052 re-sequenced behind T-055 because the breaker cannot be wired to a state machine that is about to move, T-056), and promoted **TM-001 R-6 to T-056**: tokens from two independent public TSAs, committed as offline fixtures with provenance, every incompatibility recorded, no network in CI, and park rather than substitute another local CA — because every cryptographic success so far, case 1 included, has had Mizan at both ends. Limitation: I did not re-run the live-PostgreSQL gates or `make check` in this pass, so CODEX's report of those is accepted rather than reproduced; the V-16 store semantics are reproduced from the migration by hand rather than against a live table, which is the right shape for a gate that must run without Postgres but is one step removed from the real `ON CONFLICT`; and I did not sample T-049/T-050's pre-fix SHAs, taking the gate's own case 3/4 transition as the demonstration. Hostile DB+signing-key answer **still no** — now for one wiring reason, not two reporting ones · next: CODEX T-055, then stop and re-run CP-B
- 2026-08-26 · CLAUDE · T-027 draft · Drafted `threat-models/TM-001-control-plane-v1.md` — eight adversaries stated as what they *hold* rather than as personas, seven trust boundaries, a terse control map that marks honestly which rows this document independently exercised (four: the three CP-B cases plus T-032's offline guarantee) versus which are asserted by SPEC and unsampled, and a **residual register as the load-bearing section**: what a competent adversary still achieves after every control works as designed; found and queued **T-054** while writing it — the audit commitment HMAC key has a full contract (ADR-004 *"held under separate authority"*, `MIZAN_AUDIT_HMAC_KEY_REF`, a registered rotation interval, a correct implementation in `redaction.py`) and **no custody**: it is not one of Amendment G.1's four `KeyRole`s, `keys.py:65` enforces that set literally, so the key has no KMS/HSM adapter, no production refusal of development custody, no keyset publication, and no implemented rotation — a fifth key doing real work outside the provider hardened at the checkpoint whose subject was key custody, and I-12's dictionary-attack defence is only as good as it; also recorded R-1 selective disclosure (A-8) as the largest residual and **not closable by cryptography inside Mizan** — pre-chain omission has no downstream witness, which the verifier's own `NOT COVERED` line already concedes; R-4 makes the no-bundles-leave-the-building hold a written process control rather than a remark in a review; §6 asks the owner to ratify exactly three judgement calls and says explicitly not to ratify by silence; limitation: this is a CLAUDE draft of a HUMAN-lane task and H-4 is not waived by its existing, R-7 (Redis/Kafka/object store) is deferred to TM-002 as a recorded decision rather than analysed, and the eleven control-map rows marked *not exercised* were read, not re-run · next: HUMAN ratification of TM-001 §6; T-054 queued behind T-053
- 2026-08-26 · CLAUDE · CP-B handoff · Issued `docs/handoff/CODEX-CP-B-REMEDIATION.md` (T-049..T-053, per-task file:line specs, sequence, stopping points, and what the CP-B re-run will do) and promoted R-007's validation harness into the tree as `docs/reviews/reproductions/R-007-cpb-attestation.py` — five cases, outside `tests/` so pytest does not collect it and CI does not go red on findings open by design; case 1 (real token, real bundle, operator root) and case 2 (unrelated CA rejected) are **green** regression guards, and case 1 is the only place in the tree executing both halves of the signer/verifier digest agreement; cases 3/4/5 are **red** and are the acceptance gate for T-049/T-050/T-051; case 4 now reproduces V-14 live over HTTP — a local server answers a timestamp query with `b"not-a-timestamp-token"` and the control plane records `status: "attested"` on it, which is a stronger demonstration than the prose finding; while writing the order found that **V-11 is two defects, not one** — `verify_evidence_export.py:224-230` and `evidence_export.py:109-117` rank `verified_external` above `pending` within a single anchor, so unioning the lists alone still derives `rfc3161` for `[attested_A, pending_B]`; recorded as a dated addendum in R-007 §3 rather than folded silently into the fix, and T-049's queue row rewritten to carry both halves; also caught and fixed two defects in my own harness before committing it (case 2 asserted on stdout when the verifier writes `FAIL:` to stderr, and case 4 passed on a `ConnectionResetError` because the stub never drained the request body — a false green of exactly the kind this review exists to catch) · next: CODEX T-049, then T-050, then stop and re-run CP-B
- 2026-08-26 · CLAUDE · R-007/CP-B · Independently validated `94bb25e` and did **not** pass CP-B; T-036 held in REVIEW. Re-ran `make check` (five drift gates) and 163 unit/property tests; stood up a local standards-compliant OpenSSL TSA, minted a real RFC 3161 token over the digest the verifier independently reconstructs, and ran `verify_evidence_export.py` as a subprocess over a real bundle with **no monkeypatching on either side** — PASS, `ANCHOR 0 ATTESTATION: RFC3161`, limitation line correctly withdrawn, and an unrelated CA rejected cleanly; this is the first execution of both sides of the digest agreement, which every committed test stubs on one side or the other; sampled rule-9 by `git archive`-ing the `2e4e81e` golden bundle and confirming today's verifier rejects it with exactly `manifest assurance claim does not match verified attestations`; confirmed T-032's offline guarantee survives with `mizan_control_plane` unimportable, and that the sidecar migration enforces append-only by trigger **and** grant under forced RLS; raised V-11 (mixed-**authority** streams reported externally anchored — reproduced, exit 0, contradicts ADR-004 G.11 in the same commit) and V-14 (`attested` written on unvalidated bytes) as CP-B blockers, plus V-12 (uncaught `FileNotFoundError`, four causes one message, RFC 3161 path never in CI), V-13 (worker/breaker/SLO config have no production callers — failing safe, and T-026 is the same shape), V-15 (test asserts less than its name); closed V-3 at the verifier and re-opened it inside the boundary as V-14, closed V-10 per anchor and re-opened it per authority as V-11, reclassified V-8/V-9 out of H-7 as engineering tasks; queued T-049..T-053 and added standing rules 11 and 12; limitation: real-TSA **network** interop still unexercised (I used a local TSA and, like CODEX, did not reach a public authority), and I did not run the live-PostgreSQL gates in this pass; hostile DB+signing-key answer **still no**, now for wiring and reporting reasons rather than cryptographic ones · next: T-049 CODEX, then T-050, then re-run CP-B
- 2026-08-26 · CLAUDE-rigor/CODEX · T-036/CP-B · Implemented ratified ADR-004 G.2 with digest-only multi-authority RFC 3161 requests, append-only `anchor_attestations` asynchronous completion, pending-SLO evidence breaker independent of authorization, additive customer countersignatures, production provider/TSA startup requirements, and standalone OpenSSL verification against operator-supplied roots; assurance is derived per anchor from validated tokens, the stream takes the weakest anchor, and a stronger manifest claim fails; R-006 V-10 mixed-anchor gate reports both anchors and rejects attestation-by-omission; real local OpenSSL TSA response, digest-only egress inspection, simulated outage, worker immutability, countersignature, 163 unit/property, 12 live PostgreSQL, Ruff, golden verifier, and five drift gates pass; rule-9 gate rejected the actual old golden output from pre-fix `2e4e81e85ab3f55a72d9eb1e2b628f6ae6800939` with `manifest assurance claim does not match verified attestations`; limitation: real TSA network interoperability was not exercised (a local standards-compliant OpenSSL TSA was), and pre-chain omission/withheld-final-anchor remain unprovable; hostile DB+Mizan-signing-key answer is **yes only for every finalized anchor covered by an independently trusted RFC 3161 token**, while incomplete/pending streams remain no; CP-B carries V-8 production LocalKeyProvider custody bypass and V-9 publicly derivable development keys without silently fixing either · next: STOP for CP-B disposition; do not claim T-038
- 2026-08-25 · CLAUDE-rigor/CODEX · T-025 · Implemented ratified ADR-004 G.1 with four distinct roles, development-local and vendor-neutral KMS/HSM sign-in-place `KeyProvider` adapters, additive lifecycle keyset, production startup refusal for development/local custody, separate receipt/anchor signers, authenticated `GET /v1/audit/keys`, and metadata-preserving exports; verifier reports a cryptographically valid revoked signature as `valid signature, key … revoked at T`; providers expose active-sign/keyset only and no re-sign capability; registered seven custody keys plus endpoint in SPEC and ADR-004 G.10; exhaustive generation audit found the sole production `.generate()` at old `evidence.py:99`, while execution/degraded accepted injected keys and contained none—removed it and all test call sites, with repository grep now empty; the new lifecycle-metadata gate rejected the actual pre-fix `75fdefc0787ee088002749a229a48c84f4fbbefa` golden keyset, and production-local/rotation/revocation tests pass; 149 unit/property, 12 live PostgreSQL, Ruff, and five drift gates pass; limitation: cloud KMS vendor/backend configuration is intentionally deployment-supplied and external attestation is still absent; hostile DB+signing-key answer remains **no** · next: T-036 CODEX, then stop CP-B
- 2026-08-25 · CLAUDE-rigor/CODEX · T-033 · Added config-selected `AnchorProvider.attest(anchor_payload)` seam with exactly one `development-unattested` implementation; anchors carry a signed `attestations[]` entry labelled `none_development`/`unattested`, unknown provider names fail construction, and the standalone verifier rejects absent/mislabelled state while printing `ATTESTATION: UNATTESTED`; registered `MIZAN_ANCHOR_PROVIDER`, amended ADR-004 G.2/G.9, and replaced I-11's false external-integrity assertion with a dated T-036 waiver; the new verifier rejected the real pre-provider golden bundle emitted at pre-fix `807e23df95be03b545d3b593241724ae0a6e3d67` with `anchor 0 attestation status is missing`, and the committed golden bundle was regenerated; 145 unit/property, 12 live PostgreSQL, Ruff, and all five reachability/drift gates pass; limitation: no external provider or custody change was made, by scope; hostile DB+signing-key answer remains **no** · next: T-025 CODEX
- 2026-08-25 · TEST-rigor/CODEX · T-041 · Benchmark writer now derives HEAD itself, permits `MIZAN_BENCHMARK_COMMIT_SHA` only as an exact HEAD assertion, records pre-write `worktree_clean`, and provenance validation rejects dirty artifacts plus SHAs that do not resolve to repository commits; registered both `MIZAN_BENCHMARK_*` keys in SPEC §8 under the post-hoc H-3 config rule and upgraded the accepted chain artifact with its previously logged clean state; the new validator rejected the actual pre-fix artifact from `153f676af3af54e09539ddc37c427ba6146f9281` with missing `worktree_clean` (exit 1), while forged-SHA and dirty-worktree regression cases now reject; 144 unit/property, Ruff, five baseline drift gates, and the committed artifact validator pass; limitation: Git-object provenance proves what checkout was measured, not that the committer or host is independently trustworthy; hostile DB+signing-key answer remains **no** · next: T-033 CODEX
- 2026-08-25 · CLAUDE-rigor/CODEX · T-042 · Offline verifier now binds every anchor terminating inside the export to that record hash and requires the signed anchor immediately before a non-genesis range to pin the first record's `prev_hash`; PASS output explicitly labels unsigned checkpoints as a performance aid rather than evidence; SPEC §10 and ADR-004 G.8 amended; two validly signed hostile bundles (wrong intermediate head and self-asserted left edge) were accepted by the verifier on pre-fix `de5c1e599611fc9bf25dbc55974dd1aabae08c2b`, causing both new gates to fail `assert 0 == 1`, and now reject distinctly; 142 unit/property, Ruff, and five baseline drift gates pass; limitation: a complete final anchor can still be withheld and self-signing remains inside Mizan until T-036; hostile DB+signing-key answer remains **no** · next: T-041 CODEX
- 2026-08-25 · CLAUDE-rigor/CODEX · T-043 · Added installed `mizan-export-evidence` entry point accepting an explicit runtime DSN/object root/public keyset/tenant/stream/output/range and no private key, with SPEC §10 plus ADR-004 G.7 contract deltas; added live isolated-tenant PostgreSQL gate that authorizes two requests, drains two outbox records, anchors them, invokes the exporter as a subprocess, then invokes the standalone verifier as a second subprocess over the produced bundle; on pre-fix `4aae6ac66972b38739fec0f4640ce39a0561334e` the old module returned success but emitted no path and produced no operator bundle, so the new gate failed at `assert '' == <bundle path>` rather than ImportError; implementation passes 140 unit/property, 12 live PostgreSQL, Ruff, and five baseline drift gates; limitation: CLI invocation authorization/auditing remains a deployment control and the bundle remains self-signed until T-036; hostile DB+signing-key answer remains **no** · next: T-042 CODEX
- 2026-08-25 · CLAUDE · R-006/CP-A · Independently validated the four CP-A commits: re-ran make check (five drift gates, up from four), 140 unit/property tests, the golden bundle under `python -I` with the Mizan package stripped from `sys.path` (no `mizan` module loaded, PASS plus all three disclosure lines), and confirmed each tamper test refreshes the manifest hash so the case is not caught trivially by the checksum layer; sampled all four pre-fix SHAs and found every claim honest, `73c7de9` still carrying `constraints=doc.get("constraints")`; confirmed `policy_engine.py:166` untouched and `ADR_Record` unextended; accepted T-016/T-029/T-030/T-032 DONE and credited three unrequested improvements (export reconstructs from the object store and explicitly distrusts Postgres, `record_anchor` validates under a stream-head `FOR UPDATE` instead of `ON CONFLICT DO NOTHING`, and the verifier already prints its own limitations three tasks before T-037 requires it); raised V-1..V-7, queued T-041/T-042/T-043 ahead of T-033, bound V-3 into the T-036 row, amended H-3's config clause post-hoc, and added standing rule 9; hostile DB+signing-key answer remains **no** and is unchanged until T-036 · next: T-043 CODEX, then T-042, T-041, T-033
- 2026-08-25 · CLAUDE-rigor/CODEX · T-032/CP-A · Added manifest-bound export reconstructed from immutable receipt objects plus records/receipts/anchors/checkpoints/public keys, and a standalone verifier importing no Mizan code with one-line pins `rfc8785==0.1.4 cryptography==50.0.0`; distinct CI job installs only those declared packages then runs the golden bundle in a disabled network namespace; the new six-test suite failed to import the absent exporter on pre-fix `fea9b8932cc887417551d4c322901c9c2ec92b4b` and now passes; committed tamper cases reject with `record hash mismatch at sequence 1`, `receipt coverage missing at sequence 2`, `anchor set is empty`, and `record order mismatch: expected sequence 1, got 2`; SPEC §10/ADR-004 G.6 amended; 140 unit/property, Ruff, and make check pass; clean `/tmp/mizan-offline-verifier.QVGatr` run passed under macOS `sandbox-exec` network denial with only the two declared packages plus cryptography's `cffi`/`pycparser` transitives; initial `uv --offline` installation did not work because `cffi` was absent from cache, so installation used network before runtime isolation; hostile DB+signing-key answer remains **no**, printed as a first-class verifier limitation until T-036 · next: stop at CP-A and await disposition
- 2026-08-25 · CLAUDE-rigor/CODEX · T-030 · New anchors sign dense zero-based `anchor_number`, prior complete-payload `prev_anchor_hash`, and `covered_record_count`; additive migration adds nullable legacy-identifying columns and uniqueness/density checks, while insertion validates allocation under the existing stream-head row lock; object verification now rejects anchor-number gaps, broken links/ranges/counts, and stale terminal anchors; all three committed fixture cases failed to import the absent verifier on pre-fix `dede1e33f05903b446ddcb845aac1fb8886c89a5` and now reject distinctly (`anchor_number 1`, `stale anchor through`, `covered records 2`); SPEC §10 and ADR-004 G.5 amended; 134 unit/property, 11 live PostgreSQL, Ruff, and make check pass; limitation: legacy anchors remain readable but deliberately fail chained-anchor coverage because fabricating historical metadata would be worse; hostile DB+signing-key answer remains **no** — chaining exposes omission/replay but the hostile signer can still rebuild from genesis until T-036 adds external time · next: T-032 CODEX, then CP-A
- 2026-08-25 · TEST-rigor/CODEX · T-029 · Added one artifact writer used by all three executable benchmark modules with measurements, run parameters, CPU/core/OS/Python host data, UTC timestamp, and Git SHA; CI now runs the chain benchmark into a fresh results directory and fails if its artifact is missing or malformed; the new test failed to import on pre-fix `81f6a7517fbc240ff9f9cfd628152b94f778c525`, and committed empty fixture `tests/fixtures/benchmark_artifacts/missing/` was observed failing with `missing benchmark artifact: chain-verifier`; 130 unit/property, Ruff, and make check pass; artifact `benchmarks/results/chain-verifier-81f6a7517fbc240ff9f9cfd628152b94f778c525.json` records the real development-host run; limitation: its SHA names the unchanged timed benchmark implementation at the clean pre-task HEAD because adding the artifact necessarily changes the containing commit; hostile DB+signing-key answer remains **no** — artifact discipline makes claims reproducible but adds no independent trust boundary · next: T-030 CODEX
- 2026-08-25 · CLAUDE-rigor/CODEX · T-016 · Removed the impossible ADR_Record constraints reconstruction and made every reachable `_combine` result structurally return no constraints; Policy now forbids constraints outside CONSTRAIN/REDACT, with ADR-002 Amendment B and a fifth T-021 iff gate/negative fixture; replay divergence, schema rejection, and gate tests all failed on pre-fix `73c7de93b8cc4ff45e12562cad3ec20f7164e5a2` (non-identical JSON, invalid policy accepted, gate absent) and now pass; 128 unit/property, Ruff, and make check pass; surviving `policy_engine.py:166` reads a Policy document and model/service fields carry the nullable public response shape, never ADR_Record constraints; hostile DB+signing-key answer remains **no** because this closes response determinism, not external evidence — deferred by sequence to T-030/T-036 · next: T-029 CODEX
- 2026-08-25 · HUMAN · R-005 §8 · Ratified B-11 and B-12 together as ADR-004 Amendment G — KMS/HSM custody across four separately-held key roles, published keyset, additive rotation that never re-signs history, production refusing `local://`; RFC 3161 timestamping mandatory on every production anchor with customer countersignature additive for enterprise, multiple authorities supported, blockchain still rejected, attestation asynchronous with a breaker-backed pending SLO and operator-supplied trust roots; adopted the four falsification tests and the second founder test in `docs/product/FALSIFICATION_TESTS.md`, recording its first honest answer as **no**; unblocked T-025/T-036 so nothing now waits on a human, and queued the ADR-004 Option-2 evolution path plus the auditor-facing artifacts as T-037..T-040 · next: T-016 CODEX, then the seventeen-task Stage 3 sequence in `docs/handoff/CODEX-STAGE-3.md`
- 2026-08-25 · CLAUDE · R-005 · Critically evaluated the external "Evidence Plane First" handoff brief against the tree: accepted six findings (F-13 external anchoring is ratified in ADR-004 Amd. B and implemented nowhere — `anchor()` signs with an in-process ephemeral key, so I-11's second clause is false; F-14 anchors carry no `prev_anchor_hash`/`anchor_number`/`covered_record_count`; F-15 no provenance tri-state, so a coverage gap renders identically to an answer; F-16 no offline verifier; F-17 binding coverage never enumerated; F-18 CI has drift gates but no anti-rot gates) and rejected seven claims with evidence — the brief targets PRD v1.0 and reopens ADR-006/I-17, `rfc8785`, and the T-021 gates as if unbuilt, its Rust "verbatim" costing does not survive the language boundary, its 04.1 tiering premise is refuted by B-6's 2,725 alloc/s, and its shelve-the-control-plane sequencing would discard verified assets; queued T-029..T-036 and filed B-12 for HUMAN alongside B-11 · next: T-016 CODEX
- 2026-08-25 · CLAUDE · R-004 §6 · Independent validation of the seven Stage-2 commits: re-ran make check, Ruff, 125 unit/property, 11 live PostgreSQL, 39 contract rows, 50.92% execution coverage, four drift gates on their negative fixtures, and confirmed commit isolation with an H-3 ADR delta in each; sampled three of seven pre-fix failure claims by reverting to the named SHA and reproduced all three (T-016 3-failed/3-passed on exactly CONSTRAIN/REDACT/ESCALATE, T-017 four failures, T-019 `assert 2 == 1`); accepted T-017/T-018/T-020/T-021/T-022 DONE, returned T-016 to READY on F-11 (ALLOW policies carrying constraints reach the caller but not the ADR_Record, so an idempotent replay of one request_id returns a different body), raised F-12 (no `benchmarks/results/`, so the 6.7463s claim is terminal-only under rule 6) · next: R-005 evaluation
- 2026-08-25 · TEST-rigor/CODEX · T-022 · Replaced the over-cited integration primary evidence with focused I-1/I-9/I-10/I-23/I-25/V-19 live tests; added direct token/TTL/executor and every reachable _revalidate rejection-path unit coverage with a 50% module floor; in-memory ADRs now allocate/re-chain like Postgres; coverage rows are CI-validated against collection and capped at three citations; reconciled I-1..I-26/V-1..V-21 and API v1.3; new chain, TTL, and coverage gates failed on pre-fix `f75821e` and now pass; 125 unit/property and 11 live PostgreSQL tests pass · next: independent R-004 §6 validation
- 2026-08-25 · CLAUDE-rigor/CODEX · T-020 · Added application-terminated mTLS middleware that accepts only a CERT_REQUIRED verified peer certificate with exactly one SPIFFE URI SAN, populates client_cert_spiffe, and rejects missing/SAN-less peers with 401; no CN/DN/header identity path exists; documented listener/trust-bundle contract from README and amended SPEC/ADR-001; the new certificate test failed to import on pre-fix `91bbbe2` and now passes; lint/check, 108 unit/property, and five live PostgreSQL tests pass · next: T-022 CODEX
- 2026-08-25 · CLAUDE-rigor/CODEX · T-019 · Concurrent request-id writers now converge through exact ADR request/derived-decision uniqueness constraints, tenant-RLS re-read, and context-hash equality; unreadable/different winners are 409 and unrelated integrity faults surface; in-memory persistence mirrors the atomic uniqueness lock; unit regression failed on pre-fix `d81740d` with two ADRs and now passes; lint/check, 105 unit/property, and five live PostgreSQL tests (two threads, identical response/id, exactly one ADR) pass · next: T-020 CODEX
- 2026-08-25 · CLAUDE-rigor/CODEX · T-018 · Issuance now binds the verified requested SPIFFE executor using the same allowlist enforced at redemption; live two-executor flow redeems executor two and rejects outsiders at issue/redeem; malformed delegation evidence yields 403; replay evidence keeps separate-transaction durability on a named, bounded dedicated pool with timeout telemetry; amended SPEC config/ADR-008; three new tests failed on pre-fix `bf924ca` and now pass; lint/check, 104 unit/property, and four live PostgreSQL tests pass · next: T-019 CODEX
- 2026-08-25 · CLAUDE-rigor/CODEX · T-017 · Risk and Cedar failures now persist schema-valid system_fail_closed DENY evidence before controlled HTTP 403 (a completed refusal, not service absence); double evidence failure alone returns 503 with a dedicated counter and critical alert, and persistence translation catches only expected store errors; amended SPEC/ADR-003 and I-8/V-15 coverage; three new failure-path tests failed on pre-fix `4338ae1` and now pass; lint/check, 101 unit/property, and four live PostgreSQL tests pass · next: T-018 CODEX
- 2026-08-25 · CLAUDE-rigor/CODEX · T-016 · Implemented ratified B-10 Option A across all six decision enums: unsupported winners persist schema-valid DENY evidence then return 501 NOT_IMPLEMENTED; removed both dead ADR constraints reads and the token claim, amended SPEC/ADR-008, retained a dated T-028 constraints_hash waiver; new six-enum test failed for CONSTRAIN/REDACT/ESCALATE on pre-fix `53d1035` and now passes; lint, 97 unit/property, and four live PostgreSQL tests pass · next: T-017 CODEX
- 2026-08-25 · CLAUDE-rigor/CODEX · T-021 · Added blocking Draft 2020-12 meta-schema, I-16 typed-ID, behavioural reachability/dated-waiver, and closed ADR producibility gates with four committed negative fixtures; corrected five typed-ID schema violations with ADR-005 Amendment A; new reachability test failed on pre-fix `4267dac6ddf1c616506a38b3ec490b405e6e5b66` (NOT_IMPLEMENTED and system_fail_closed unreachable) and passes on the change; 91 unit tests and lint/check pass · next: T-016 CODEX
- 2026-08-25 · HUMAN · R-004/B-10 · Ratified Option A: evaluator rejects CONSTRAIN/REDACT/ESCALATE with `NOT_IMPLEMENTED` plus an auditable DENY ADR_Record, dead `constraints_hash` binding removed, constrained execution deferred to v1.4 (T-028 PARKED); T-016 unblocked, B-11 key custody still open · next: T-021 then T-016 CODEX
- 2026-08-25 · CLAUDE · R-004 · Reviewed the T-001..T-015 baseline against SPEC v1.3 and reproduced the lint/86-test/live-Postgres/100k-chain/Cedar/32-commit-ledger claims (sequencer ops/s not retained, superseded by T-023); dispositioned ten findings, headlined by CONSTRAIN/REDACT being unrecordable under the closed ADR_Record schema and `system_fail_closed` existing in SPEC but in zero lines of code; queued T-016..T-028 and filed B-10/B-11 for HUMAN · next: T-021 CODEX
- 2026-08-25 · CODEX · T-007 v1.3 · Closed I-1..I-26/V-1..V-21 and R-003 coverage, added fresh-review-authority property checks, reconciled ratified ADR statuses, and passed baseline/lint/JS, 86 unit/property, four live PostgreSQL, 2,708 ops/s sequencer, 6,018 eval/s policy, and 100k chain verification in 6.424s; all T-001..T-015 DONE · next: new scoped task
- 2026-08-25 · CODEX · T-009 v1.3 · Made review-triggering rejection atomically close the original epoch, snapshot the independently configured authority pool, open an isolated no-carry review epoch, append decision evidence, and enqueue review notification; 85 unit plus four live integration tests pass; task DONE · next: T-007
- 2026-08-25 · CODEX · T-006 v1.3 · Implemented the R-003 semantic policy hash, locked lifecycle endpoint, simulation-before-test and author/approver separation, UTC activation, prior-version supersession, transactional transition events, and lifecycle/hash regression coverage; task DONE · next: T-009
- 2026-08-25 · CODEX · T-011 v1.3 · Required bounded arguments on execute, recomputed the pinned profile hash before both first redemption and idempotent retry, revalidated immutable normalized context against current agent/tool/resource/risk/delegation authority, and rejected changed bound arguments before CAS/lease reuse; 82 unit plus four live integration tests pass; task DONE · next: T-006
- 2026-08-25 · CODEX · T-004 v1.3 · Added the ratified bounded transient argument contract, server-side binding validation/hash, raw-argument exclusion from policy/evidence/context hashes, and immutable tenant-RLS normalized context snapshots; 82 unit plus four live integration tests pass; task DONE · next: T-011
- 2026-08-25 · HUMAN · R-003 · Ratified B-7 typed review authority, B-8 bounded arguments/fresh execution revalidation, and B-9 semantic policy hash in all required roles · next: SPEC v1.3 + T-004 CODEX
- 2026-08-25 · CODEX · T-004 audit · Replaced permissive implementation-only context bags with exact typed customer/business/security/mapped/environment/timestamp models, removed non-schema tool/action fields, generated server trace evidence, and proved the context validates after isolating only the B-8 arguments gap; 80 unit plus four live integration tests pass; PARKED on R-003/B-8 · next: HUMAN ratify R-003
- 2026-08-25 · CODEX · T-007 audit · Reconciled the coverage index after T-011/T-012/T-013 hardening, added durable hashed-jti replay signaling, and separated security notifications from evidence-publisher outbox selection to prevent publication starvation; 79 unit plus four live integration tests pass; PARKED only where B-7/B-8 make complete invariant tests impossible · next: T-004 audit
- 2026-08-25 · CODEX · T-010 audit · Added exact tenant-RLS dashboard aggregates for all seven PRD metrics, responsive control-center cards, agent registry/detail view, active-view refresh, and retained evidence-backed action/audit/verification views; 79 unit plus four live integration tests and JavaScript syntax pass; task DONE · next: T-007 audit
- 2026-08-25 · CODEX · T-013 audit · Normalized telemetry/persistence/parser faults as controlled tool errors, moved timeout enforcement before irreversible persistence, enforced RFC 6901 pointer rules, and deduplicated truncated drift paths; 79 unit tests pass; task DONE · next: T-010 audit
- 2026-08-25 · CODEX · T-012 audit · Recomputed stored-payload hashes, verified manifest counts and concrete transforms, fixed numeric/depth ordering for array drops, and made payload-free redaction-failure outbox signaling a mandatory Redactor dependency; 75 unit plus four live integration tests pass; task DONE · next: T-013 audit
- 2026-08-25 · CODEX · T-011 audit · Enforced the complete closed token-claims schema at issue and redeem, revalidated constraints and live approval epoch, and fixed expired lease state/evidence rollback by committing before the controlled error; 74 unit plus four live integration tests pass; PARKED only on B-8 argument recomputation · next: T-012 audit
- 2026-08-25 · CODEX · T-005 audit · Corrected production/simulation selector parity using registry-enriched risk and normalized environment, added native Cedar decimal encoding/method comparisons with representability rejection, and reran 71 unit plus four live integration tests; benchmark measured 6,991 eval/s at p99 0.1703 ms; task DONE · next: T-011 audit
- 2026-08-25 · CODEX · T-006 audit · Added authenticated agent lifecycle PATCH with real two-token dual control, normative transition graph, versioned immutable binding-profile publication, recorded policy simulation using the production compiler, delegation-edge persistence, and live tests; PARKED policy transition endpoint on content-hash contradiction B-9 · next: T-005 audit
- 2026-08-25 · CODEX · T-003 audit · Removed runtime UPDATE/DELETE grants from immutable evidence, corrected `dgn_*` nonce storage, added receipt/anchor chain FKs, token↔lease relational bindings, approval-state/document checks, and a separately mounted rollback migration with destructive disposable-db verification; live schema/repository/rollback gates pass; task DONE · next: T-005/T-006 audit
- 2026-08-25 · CODEX · T-008/T-015 audit · Replaced helper-only perf proof with the real receipt/object/anchor verifier; deduplicated segment reads, parallelized 100k Ed25519 receipt checks, required matching WORM anchors, handled missing/malformed evidence as controlled failures, and corrected shard default 16→4; actual 100k path passes in 6.409s; 61 unit + four live integration tests pass; both tasks DONE · next: T-003 audit
- 2026-08-25 · CODEX · T-014 · Release audit found CI lacked uv installation and never ran lint/unit/invariant/performance gates; added locked setup-uv jobs for Ruff, 60 unit/property tests, 100k chain benchmark, and live Postgres suite; local equivalents pass; task independently audited DONE · next: T-008 audit
- 2026-08-25 · CODEX · T-007 · Added Hypothesis chain/representability/approval fuzzing and I-1..I-26/V-1..V-21 coverage index; closed delegation, quorum, policy dual-control, binding unknown-field, DecisionEvent retry, external receipt validation, audit-attestation and encrypted degraded-WAL gaps; 60 unit + four live integration tests pass; B-7/B-8 remain contract decisions · next: HUMAN B-7/B-8
- 2026-08-25 · CODEX · T-010 · Added responsive same-origin operator console for decision filters/details, audit browsing and signed-chain verification; implemented missing tenant-RLS decision/audit query endpoints with cursor pagination and live integration assertions; 43 unit + four live integration tests pass · next: T-007 CODEX
- 2026-08-25 · CODEX · T-015 · Added independently anchored checkpoint-range verification with parallel workers and boundary-continuity checks, deterministic 100k fixture, corruption tests and Make target; M3 Max verified 100k in 0.917s vs <10s gate; 43 unit tests pass · next: T-010 CODEX
- 2026-08-25 · CODEX · T-013 · Added bounded streaming identity/gzip decoding, duplicate/non-finite JSON rejection, depth/key/time budgets, SPEC-valid transient envelopes, scalar-only versioned projections, drift telemetry, explicit raw-persistence sinks and operational payload stripping; 42 unit tests pass · next: T-015 CODEX
- 2026-08-25 · CODEX · T-012 · Added rule-based DLP scanner interface, fail-closed redactor, HMAC source/field commitments, stored-payload hash, coverage attestation and manifests, plus atomic redacted AuditTrail/outbox writes; 34 unit + four live integration tests pass · next: T-013 CODEX
- 2026-08-25 · CODEX · T-011 · Added EdDSA execution capabilities, issuer/audience/TTL enforcement, DB-backed single-use CAS, executor SPIFFE binding, agent/context/profile revalidation, financial receipt gating, idempotent leases, bounded heartbeats/completion and same-transaction DecisionEvents; 30 unit + live lifecycle integration tests pass; B-8 remains the argument-recomputation contract gap · next: T-012 CODEX
- 2026-08-25 · CODEX · T-009 · Implemented approval domain/API/storage for eligibility snapshots, control-domain quorum, immutable per-epoch votes, veto/rejection quorum, stale-race rejection, escalation, override, withdrawal and same-transaction DecisionEvents; 28 unit + live integration tests pass; PARKED review_required completion on missing review-authority contract B-7; also recorded T-004 argument-binding contract gap B-8 · next: T-011 CODEX
- 2026-08-25 · CODEX · T-008 · Added dense dual-sequenced DecisionEvents, transactional outbox publisher, RFC8785 immutable segments, append-only Ed25519 receipts, signed anchors, independent object verifier and audit routes; unit/live integration tests pass; four-shard benchmark reached 2,725 ops/s at p99 2.0087 ms; accepted ADR-004, resolved B-6, unblocked T-012/T-015 · next: T-009 CODEX
- 2026-08-25 · CODEX · T-014 · Replaced disabled CI placeholder with per-commit claim-ledger enforcement; scoped commits must update WORK_LOG and contain one live claim or a newest matching REVIEW/DONE handoff; unit tests and full-history validation across seven commits pass · next: T-008 CODEX
- 2026-08-25 · CODEX · T-006 · Added exact SPEC JSON-Schema validation, tenant-scoped agent/tool/policy create/get/list endpoints, canonical content/profile hashes, allowlisted SQL resource mapping, latest-policy selection and keyset cursors; 13 unit and three live Postgres integration tests pass · next: T-014 CODEX
- 2026-08-25 · CODEX · T-005 · Added Cedar 4.8.7 DSL compiler, safe condition translation, immutable PolicySet cache, applies-to selection, Postgres ACTIVE-policy integration, parity/adversarial tests and benchmark; measured 6,896 eval/s at p99 0.1741 ms plus RLS lookup p99 gate <50 ms; accepted ADR-002 and resolved B-2 · next: T-006 CODEX
- 2026-08-25 · CODEX · T-004 · Added FastAPI authorization core with asymmetric JWT verification, identity-derived tenancy, registry/resource/binding enrichment, RFC8785 hashes, fail-closed risk/evidence handling, deterministic idempotency, default DENY, atomic ADR+outbox Postgres adapter, six unit tests and live RLS-scoped integration round-trip; released claim and unblocked T-007/T-009/T-011/T-013 · next: T-005 CODEX
- 2026-08-25 · CODEX · T-003 · Added PostgreSQL 16 schema/rollback for all SPEC §2 persisted models, typed ID domains/composite FKs, forced RLS on 21 tenant tables, immutable evidence triggers, transaction-safe evidence/DecisionEvent heads, Docker integration profile, and CI contract test; all checks pass; released claim and unblocked T-004/T-006/T-008 · next: T-004 CODEX
- 2026-08-25 · HUMAN · GOVERNANCE · Assigned CODEX as sole implementation/test agent across former CLAUDE/CURSOR/TEST lanes; lane labels now express required rigor, while HUMAN decision gates remain intact; accepted T-002 · next: T-003 CODEX (claimed)
- 2026-08-25 · CURSOR · T-002 · Initialized Git with ratified root baseline; scaffolded PRD §116 product boundaries, lane markers, repository hygiene, dependency-free contract validator, and GitHub Actions CI skeleton; `make check`, Python compile, and whitespace checks pass; released claim and unblocked T-014 · next: T-003 CLAUDE
- 2026-08-25 · HUMAN · T-001 · Ratified SPEC v1.2, R-002, and ADR-001..008 in all required Product/Architecture, Cybersecurity, and Compliance/Business roles; unblocked T-002/T-003/T-005 · next: T-002 CURSOR (claimed)
- 2026-08-25 · CURSOR · T-000c · Applied re-audit R-002: SPEC v1.2 executor-bound capabilities, default-deny evidence, DecisionEvents, immutable receipts, trusted degraded grants/WAL contract, external parser/persistence limits; amended ADR-001/002/003/004/006/007/008; blocked implementation pending ratification · next: T-001 HUMAN
- 2026-08-25 · CLAUDE · T-000b · Applied review R-001: SPEC v1.1 (typed IDs, enrichment, epochs, binding profiles/leases, redaction attestation, config registry §8, V-rules §9, evidence pipeline §10), ADR-006/007/008, ADR-003/004 amendments, H-1 claim leases + H-8 CI gate · next: human ratification (T-001), then T-002
- 2026-08-25 · CLAUDE · T-000 · Generated SPEC_v1.md, ADR-001..005, WORK_LOG.md, AGENT_ALLOCATION.md from PRD v1.0 · next: human ratification (T-001)
