[project]
name = "mizan-control-plane"
version = "0.1.0"
description = "Governance control plane for regulated autonomous agents"
requires-python = ">=3.12"
dependencies = [
  "cedarpy==4.8.7",
  "fastapi>=0.115,<1",
  "httpx>=0.27,<1",
  "jsonschema[format]>=4.23,<5",
  "psycopg[binary,pool]>=3.2,<4",
  "pydantic>=2.10,<3",
  "pyjwt[crypto]>=2.10,<3",
  "rfc8785>=0.1.4,<1",
  "uvicorn[standard]>=0.34,<1",
]

[project.scripts]
mizan-export-evidence = "mizan_control_plane.evidence_export:main"
mizan-attest-anchors = "mizan_control_plane.attestation_runner:main"

[dependency-groups]
dev = [
  "hypothesis>=6.130,<7",
  "pytest>=8.3,<9",
  "pytest-cov>=6,<7",
  "ruff>=0.9,<1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["control-plane", "security", "integrations", "."]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["control-plane/mizan_control_plane", "security/mizan_security", "integrations/mizan_integrations"]
